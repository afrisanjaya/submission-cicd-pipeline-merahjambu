pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    docker.build('golang-app', '-f Dockerfile.builder .')
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    docker.image('golang-app').inside {
                        sh 'go version'
                        sh 'go test ./...'
                    }
                }
            }
        }
    }
}
